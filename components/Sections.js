import TextSection from "./TextSection"

export default function Sections({section}) {

  // console.log(section);

  return (
    // {section.map((singleSection, index) => {

    //   <TextSection section={singleSection} key={index} />
    // }
    <p>test</p>
  )
}
